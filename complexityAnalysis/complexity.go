package main()

import "fmt"

// main func contains a linear algorithm that multiplies the nth index with 200 and moves to n+1.
func main() {
    var arr [10]arr
    var k int
    for i = 0; i <= len(arr); i++ {
        arr[k] = k * 200
    }
    fmt.Printf("at %s, put %s", arr[k])
}

//this takes exactly O(n) time to complete